{
    /* Bintray package information.
       In case the package already exists on Bintray, only the name, repo and subject
       fields are mandatory. */

    "package": {
        "name": "pitstop",
        "repo": "releases",
        "subject": "wazim"
    },


    "version": {
        "name": "release"
    },

    "files":
        [
        {"includePattern": "/home/travis/build/wazim/pitstop/target/(.*\.jar)", "uploadPattern": "$1"},
        {"includePattern": "/home/travis/.m2/repository/net/wazim/pitstop/(.*)/(.*\.jar)", "uploadPattern": "$2"}
        ],
    "publish": true
}